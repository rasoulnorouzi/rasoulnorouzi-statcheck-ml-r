% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{sc_check}
\alias{sc_check}
\title{Read a PDF and check every statistical result in it}
\usage{
sc_check(path, kit = sc_kit(), model = sc_load_model(kit), timeout = 60)
}
\arguments{
\item{path}{A PDF file.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}

\item{model}{A model from \code{\link[=sc_load_model]{sc_load_model()}}. Loading it costs about a
second, so a caller checking many documents should load it once and
pass it to every call.}

\item{timeout}{Seconds to allow reading the PDF. Passed to
\code{\link[=sc_read_pdf]{sc_read_pdf()}}; ignored when R.utils is not installed.}
}
\value{
The \code{\link[=sc_check_text]{sc_check_text()}} result for the document's text, with
\code{source_file} added as the first column, holding \code{path} in every row.
Zero rows when the document holds nothing checkable or could not be
read. The \code{"stages"} attribute is kept.
}
\description{
Runs \code{\link[=sc_read_pdf]{sc_read_pdf()}} then \code{\link[=sc_check_text]{sc_check_text()}}: the whole pipeline from a
PDF file on disk to a table of checked results. Every result carries the
file it came from in \code{source_file}, so results from several documents can
be row-bound into one table without losing where each came from.
}
\examples{
\dontrun{
kit <- sc_kit()
sc_check("article.pdf", kit)
}
}
