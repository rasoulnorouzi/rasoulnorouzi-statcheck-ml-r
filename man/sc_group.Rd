% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group.R
\name{sc_group}
\alias{sc_group}
\title{Group tagged spans into reported results}
\usage{
sc_group(text, tags)
}
\arguments{
\item{text}{The text \code{tags} was produced over -- the same text a
\code{\link[=sc_tag]{sc_tag()}} case stores, one tag per character.}

\item{tags}{A character vector of BIOES tags, as long as \code{text} has
characters.}
}
\value{
A data frame with one row per result found: \code{test_type},
\code{statistic}, \code{df1}, \code{df2}, \code{p_operator}, \code{p_value}, \code{start}, \code{end}
(0-based, half-open). Zero rows when no result groups.
}
\description{
Ports \code{evalutil.group_spans} and \code{evalutil.build_result} together: reads
spans out of \code{tags}, groups them into one result per test, and parses
each part's raw text into a number.
}
\examples{
kit <- sc_kit()
tags <- sc_tag("F(2, 30) = 4.11, p = .03", kit)[[1]]
sc_group("F(2, 30) = 4.11, p = .03", tags)
}
