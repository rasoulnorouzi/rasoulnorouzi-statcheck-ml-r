% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{sc_extract}
\alias{sc_extract}
\title{Find every reported test result a pattern can read}
\usage{
sc_extract(text, kit)
}
\arguments{
\item{text}{A window of text, already normalised with \code{\link[=sc_normalize]{sc_normalize()}}.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}. Reserved for a future rule that reads the
kit; the pattern itself is fixed and ships with the package.}
}
\value{
A data frame with one row per result found, ordered by where it
starts in \code{text}, and the columns \code{test_type}, \code{statistic}, \code{df1},
\code{df2}, \code{p_operator}, \code{p_value}, all character. Zero rows when nothing
matches.
}
\description{
Reproduces what the R package statcheck finds: a test statistic reported
in APA style, with its degrees of freedom and its p-value. This is the
pattern-based baseline, not the trained model, and it keeps the faults of
the version it mirrors so that improvement can be measured against it.
}
\details{
A result whose degrees of freedom or p-value the text does not report,
such as a z test's \code{df1}, comes back as \code{NA} in that column. \code{df1},
\code{df2}, and \code{p_value} hold the raw digits as they were written, so a
leading zero or its absence survives (\code{.03} next to \code{0.010} is expected).
}
\examples{
kit <- sc_kit()
sc_extract("The main effect was significant, F(2, 87) = 4.11, p = .03.", kit)
}
