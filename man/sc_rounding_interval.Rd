% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pvalue.R
\name{sc_rounding_interval}
\alias{sc_rounding_interval}
\title{The p-values a statistic could imply, given how it was rounded}
\usage{
sc_rounding_interval(result, statistic_text = NULL)
}
\arguments{
\item{result}{A list describing one reported result, as in \code{\link[=sc_verdict]{sc_verdict()}}.}

\item{statistic_text}{The statistic exactly as printed, such as \code{"1.48"}.
When it is not given, the decimals are read from \code{result$statistic}
itself, formatted with \code{format(x, scientific = FALSE)} -- \code{2.45} gives
2 decimals and \code{5.1} gives 1, which is right whenever the number was
parsed from the text it was printed as. A whole number loses its
trailing zero this way (\code{5} reads as 0 decimals, not the 1 a paper's own
"5.0" would have printed), so a caller that has the printed text should
always pass it.}
}
\value{
A list with \code{low_p} and \code{up_p}, or both \code{NA_real_} when no p can
be computed for either end of the interval.
}
\description{
A paper writes \code{t(67) = 1.48}. The true statistic is anywhere in
\verb{[1.475, 1.485]}, and each end implies a different p-value. statcheck
compares the reported p against that whole interval (\code{error_test} in
statcheck 1.5.0), and this port must do the same or it calls a correctly
reported result an error.
}
\examples{
sc_rounding_interval(list(test_type = "t", statistic = 1.48, df1 = 67),
                     statistic_text = "1.48")
}
