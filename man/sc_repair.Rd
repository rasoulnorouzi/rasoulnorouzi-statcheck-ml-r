% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair.R
\name{sc_repair}
\alias{sc_repair}
\title{Repair operators a PDF conversion destroyed}
\usage{
sc_repair(text, kit)
}
\arguments{
\item{text}{A document, already normalised with \code{\link[=sc_normalize]{sc_normalize()}}.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}
}
\value{
A list with \code{text} (the repaired document) and \code{replacements}
(how many operator characters were changed).
}
\description{
Tries every plausible reading of a document's damaged operator characters
and keeps the one whose recomputed p-values agree with the reported ones
most often -- a test that needs no annotator, only the p-value
mathematics in \code{\link[=sc_compute_p]{sc_compute_p()}}. When too few results in the document can
be tested this way, falls back to a simpler rule: the character right
after a set of degrees of freedom is always an equals sign.
}
\examples{
kit <- sc_kit()
sc_repair("F(1, 214) \x01 50.54, p \x01 .001", kit)
}
