% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kit.R
\name{sc_kit}
\alias{sc_kit}
\title{Load and verify a port kit}
\usage{
sc_kit(path = system.file("kit", package = "statcheckml"))
}
\arguments{
\item{path}{Directory holding \code{manifest.json}, \verb{spec/}, and \verb{model/}.
Defaults to the kit shipped inside the installed package.}
}
\value{
An \code{sc_kit}: a list with \code{spec} (one parsed JSON per file in
\verb{spec/}, named after the file), \code{model_dir} (the default model's
directory), \code{manifest} (the parsed \code{manifest.json}), and \code{path}.
}
\description{
A kit holds the shared rules and a trained model that a statcheck-ml port
needs: the prefilter, the normalisation rules, the character vocabulary,
and the p-value constants, all in \verb{spec/}, plus one or more models in
\verb{model/}. The mother repository writes the kit; a port only reads it.
}
\details{
Every file the kit's \code{manifest.json} lists is hash-checked before
\code{\link[=sc_kit]{sc_kit()}} returns, so a copy that has drifted from the mother repository
is refused rather than silently trusted.
}
\examples{
kit <- sc_kit()
kit$spec$normalize$target_line_width
}
