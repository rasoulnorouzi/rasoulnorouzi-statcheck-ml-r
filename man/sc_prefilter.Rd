% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prefilter.R
\name{sc_prefilter}
\alias{sc_prefilter}
\title{Select the windows of text a model or pattern should read}
\usage{
sc_prefilter(text, kit, drop_references = TRUE)
}
\arguments{
\item{text}{A document, already normalised and repaired.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}

\item{drop_references}{Blank the reference section before filtering.}
}
\value{
A data frame with one row per kept window: \code{start} and \code{end}
(character offsets into \code{text}), \code{line} (the 0-based index of the line
that triggered the window), and \code{text} (the window's own text, built
from the reference-blanked lines). Zero rows when nothing survives.
}
\description{
Applies the shared prefilter rules: a line survives only if it is long
enough, holds a digit, and is dense enough in non-letter characters, and
the reference section is blanked out first. Each surviving line becomes a
window that carries the lines around it, because a result can be split by
a line break.
}
\examples{
kit <- sc_kit()
sc_prefilter("The effect was significant, t(28) = 2.87, p = .006.", kit)
}
