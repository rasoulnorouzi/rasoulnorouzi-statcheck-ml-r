% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{sc_read_pdf}
\alias{sc_read_pdf}
\title{Read one PDF and return normalised text}
\usage{
sc_read_pdf(path, kit, timeout = 60)
}
\arguments{
\item{path}{A PDF file.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}

\item{timeout}{Seconds to allow one document. Ignored when R.utils is
not installed.}
}
\value{
Normalised text, or \code{""} when the document could not be read.
}
\description{
R has no maintained PDF reader besides pdftools, which bundles poppler.
Poppler returns a whole column as one line, which is why every result of
\code{\link[=sc_read_pdf]{sc_read_pdf()}} passes through \code{\link[=sc_normalize]{sc_normalize()}} before it comes back.
}
\details{
pdftools hangs on rare files. When R.utils is installed, a document that
does not finish within \code{timeout} seconds returns \code{""} instead of
stopping the caller; without R.utils no limit applies, and this is
reported once with a warning.
}
\examples{
\dontrun{
kit <- sc_kit()
sc_read_pdf("article.pdf", kit)
}
}
