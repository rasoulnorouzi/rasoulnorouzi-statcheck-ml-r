% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{sc_tag}
\alias{sc_tag}
\title{Tag the characters of a text with the trained model}
\usage{
sc_tag(texts, kit, model = sc_load_model(kit))
}
\arguments{
\item{texts}{A character vector.}

\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}

\item{model}{A model from \code{\link[=sc_load_model]{sc_load_model()}}. Loaded from \code{kit} when it is
not given, which is worth avoiding in a loop.}
}
\value{
A list of character vectors, one per text, each as long as its
text has characters.
}
\description{
Returns one tag per character, in the BIOES scheme the mother repository
defines. The text must be the text the model reads: \code{sc_tag()} runs no
normalisation of its own, because the caller decides whether a window
comes from \code{\link[=sc_normalize]{sc_normalize()}} or from somewhere else.
}
\details{
Every text given in one call is tagged in one batch, so tagging a whole
document's windows together costs little more than tagging one of them.
A shorter text is padded to the longest, and the padding changes no
text's tags.
}
\examples{
kit <- sc_kit()
sc_tag("F(2, 30) = 4.11, p = .03", kit)[[1]]
}
