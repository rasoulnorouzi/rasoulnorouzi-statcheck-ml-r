% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{sc_load_model}
\alias{sc_load_model}
\title{Load a trained tagger from a kit}
\usage{
sc_load_model(kit, config = kit$manifest$model_default)
}
\arguments{
\item{kit}{A loaded \code{\link[=sc_kit]{sc_kit()}}.}

\item{config}{Which model in the kit to load. Defaults to the kit's own
default model.}
}
\value{
An \code{sc_model}.
}
\description{
Reads the model's \code{weights.json} -- the embedding table, both directions
of both recurrent layers, the tag projection, and the CRF -- and returns
everything \code{\link[=sc_tag]{sc_tag()}} needs. Loading a model costs a second or so, so a
caller that tags many documents should load it once and pass it on.
}
\examples{
model <- sc_load_model(sc_kit())
model
}
